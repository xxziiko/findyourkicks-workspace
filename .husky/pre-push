#!/usr/bin/env sh

# pre-push: 빌드 검증만 수행
# QA (agent-browser, Playwright, Vitest)는 CI에서 PR 단위로 실행

echo "🔍 프로젝트 빌드 중..."
if pnpm run build; then
  echo "✅ 빌드 완료."
else
  echo "❌ 빌드 실패."
  exit 1
fi

echo "🎉 pre-push 검사 완료!"
exit 0
